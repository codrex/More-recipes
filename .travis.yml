language: node_js

node_js: "7"

install:
- npm install

script:
- npm test

env:
- NODE_ENV=test

cache:
  directories:
  - "node_modules"

services:
- postgresql

before_script:
- createuser more-recipe -s -U postgres
- createdb moreRecipesTest --username=postgres
- sequelize db:migrate

notifications:
  webhooks: https://coveralls.io/webhook repo_token=D1KMLZwoX449H7ufQwSs30jYLOTuvC6em
